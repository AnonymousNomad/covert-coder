node_modules/
desktop/frontend/
desktop/target/
models/*.gguf
models/*.safetensors
*.log
logs/
artifacts/veritas.md
.DS_Store
.env
.env.*
!.env.example
community/store.local.json

.aide/
models/*.corrupt
styles.css.wrongfile


browser/dist/
build-out.txt
check-out.txt
vite-out.txt
daemon-out.txt
dom*.txt
test-results/
playwright-report/


*.pyc
skills/packs/**/__pycache__/

*.profile.json

dist/
legacy-shell-backup/
live-stack.*
arch-out.txt
arch-err.txt
models/tinyllamas/

models/aide-house/
.aider*

# Closed-loop self-improvement: training signal contains the user's failures on
# the user's tasks. Local-only per the No-Phone-Home law.
.aide/training/signal-*.jsonl

# North-Mini-Code-1.0 in-house model GGUF (10.5GB). Managed by AIDE's model
# engine, not git. Symlink in models/aide-house/ is the engine's expected path.
models/north-mini-code/

# Nested private project (separate repo with its own .git) living in the
# workbench workspace. Never part of this repository.
FSI/

# Desktop packaged-stack staging: full mirror of the source tree + node_modules
# for the offline stack launcher. Regenerated per build, like desktop/frontend/.
desktop/resources/

# Tauri CLI generated schemas; regenerated from desktop capabilities/config.
desktop/gen/schemas/
