# CAPABILITY WIRING LEDGER — Covert Coder / AIDE Sovereign Workbench

Audit target: `covert-production` @ `8ea6c8b` ("docs: align public surfaces with covert coder")
Machine-readable: `artifacts/integration-certification/capability-ledger.json`
Generated by: wiring-audit lane (`audit/wiring-ledger`) — 2026-09-22

> Production note: Ghost Code, the provider bridge (moonshot/kimi/opencode), and the
> subscription transports beyond codex/claude exist ONLY on release branches and are
> **NOT_IMPLEMENTED in production** until a merge decision is made.

## Classification counts (first pass, 38 capabilities)

| Classification | Count |
|---|---|
| FULLY_WIRED | 12 |
| PARTIALLY_WIRED | 13 |
| ORPHANED | 5 |
| SHADOWED | 2 |
| EXPERIMENT_ONLY | 1 |
| UI_ONLY | 1 |
| BACKEND_ONLY | 4 |
| DEAD | 3 |
| NOT_IMPLEMENTED | 5 |
| P0 | 1 (plus 3 P0 items tracked in `p0_list`) |

## P0 LIST (production, must not be ignored)

1. **Detached self-improve runner has no authority and no audit** — spawned at boot and every 6h, `stdio:'ignore'`, unref'd; writes workspace signal/journal files (`server.ts:382-399`, `scripts/selfimprove.mjs:121-196`).
2. **Release verification gate accepts abstained/fabricated records** — blocks only explicit failure markers; `verification.state=abstain` passes; records are unauthenticated JSON; `.aide/` writes are approval-only risk flags (`workflow-service.ts:275-279`, `workflow-validators.ts:217-236`, `agent-tools.mjs:9,83-88`).
3. **External egress auto-approved as `capability.read`** — cloud chat via BYOK and HF search with token require no operator decision binding external transmission (`operation-policy.mjs:86-92`, `routes/modelhub.ts:57-68`).
4. **Shadowed direct tool dispatcher** — `createAgentTools` without authority; safety depends on `readOnly` labels; no trajectory/verification evidence (`openapi.ts:788-808`).

## Ten headline findings

1. **Two disconnected cognition systems**: chat receives the canonical envelope (awareness, memory, RAG, skills); the agent loop — the only path that plans/codes/verifies — receives raw resident context + skills only (`chat-context.ts:106-219` vs `agent-loop.mjs:286-298`).
2. **Micro-experts are advisory text, never decision inputs**: no production branch consumes expert output; the in-loop advisory is gated on `chat_source='provider'` which the sovereign UI never sends (`agent.ts:107-143` vs `ResidentCore.ts:217`); 2 of 3 advisories have zero consumers.
3. **Workflow can be read and gated but never instantiated**: `create()` has zero production callers; no agent/chat/model code branches on stage (`workflow-service.ts:351`, absent `.aide/workflow/state.json`).
4. **Skills stage routing is dead**: loader reads a `stage` field no caller supplies; registry contains 0 stage fields (`skills-loader.mjs:100`).
5. **Handoff bundles are write-only**: accepted, stored, never consumed into context or execution (`handoff-service.mjs:287-298`).
6. **Agent sessions do not survive restart**: sessions, transcripts, pending approvals are an in-memory Map (`agent-loop.mjs:234,706-752`); `.aide/agent-loop-sessions` is an orphaned store with no writer.
7. **Veritas battery is CLI-only**: deterministic checks exist but the live loop forces `passed:false` and the workflow gate never invokes them (`harness/checks.mjs`, `agent-loop.mjs:598-638`).
8. **Harness orchestrator is orphaned**: `harness/orchestrator.mjs` is imported only by tests/scripts (`orchestrator.mjs:36`).
9. **DAP/LSP-mutations/training/model-import are fail-closed DEAD**: unenrolled routes denied at dispatch, tracked as migration waivers (`server.ts:224`, `route-authority-coverage.test.ts:32-55`).
10. **Routing is role-only**: no task/content awareness, capability metadata is empty, hardware profile is not imported by the router, HF discovery is catalog lookup (`model-router.ts:172-186`, `modelhub.mjs:176-179`).

## Subsystem summary (condensed)

| Subsystem | Verdict | Key evidence |
|---|---|---|
| Execution Authority + policy | FULLY_WIRED | `execution-authority.mjs:16,226-316`, `operation-policy.mjs:4-147` |
| Agent loop harness | FULLY_WIRED | `agent-loop.mjs:230,435-457` |
| Terminal/PTY/Git/Tasks/Models/Desktop | FULLY_WIRED | policy rows 35-59, `server.ts:160-168` |
| Chat context composer | FULLY_WIRED | `chat-context.ts:106-219` |
| Resident routes + awareness envelope | FULLY_WIRED | `resident.ts:245,640-666`, `resident-envelope.mjs:186-267` |
| Skills registry + injection | FULLY_WIRED | `skills-loader.mjs:76-119`, `chat-context.ts:170-174` |
| Memory: history/recall/digests/audit | FULLY_WIRED | `chat-store.ts`, `memory-recall.mjs:115-133`, `memory-spine.mjs:103` |
| Micro-experts | PARTIALLY_WIRED (advisory SHADOWED) | `micro-experts.mjs:96,347`, `agent.ts:107-143` |
| Workflow | PARTIALLY_WIRED (lifecycle DEAD) | `workflow-service.ts:103-115,351` |
| Agent-loop context | PARTIALLY_WIRED (separate composer) | `agent-loop.mjs:286-298` |
| Model router | PARTIALLY_WIRED (role-only) | `model-router.ts:172-186` |
| Hardware profile | PARTIALLY_WIRED (not in router) | `hardware-profile.mjs:84-131` |
| HF discovery | PARTIALLY_WIRED (catalog-only) | `modelhub.mjs:176-179,248` |
| Subscription transports (codex/claude) | PARTIALLY_WIRED (presence-only routing) | `subscription-transports.ts:102-191` |
| Handoff | ORPHANED (bundles) | `handoff-service.mjs:287-298` |
| Harness orchestrator | ORPHANED | `orchestrator.mjs:36` |
| `.aide/agent-loop-sessions` | ORPHANED | `system-map.mjs:57-59` |
| Memory blocks writer | ORPHANED | `memory-blocks.mjs:36-50` |
| Checkpoints | PARTIALLY_WIRED (restore unused) | `agent-checkpoints.mjs:101-141` |
| Ghost Code | NOT_IMPLEMENTED in production | lane branches only |
| Provider bridge (kimi/opencode/moonshot) | NOT_IMPLEMENTED in production | lane branches only |
| Offline core parity (Part 19) | PENDING | — |
| MCP/external audit (Part 20) | PENDING | — |
| Integration battery (Part 33) | PENDING | — |

## Program status

- Part 1 archaeology: **DONE (first pass)** — three deep traces (micro-experts/skills/workflow; resident/context/memory/restart; orchestrator/authority/harness/veritas/ghost/routing).
- Part 2 ledger: **DONE (first pass, 38 rows)** — expanding in later passes.
- Part 3 dead/orphaned search: **DONE (first pass)**.
- Parts 4-6 (micro-expert probes/ablation): NEXT.
- Parts 7-13, 14-18, 19-22, 23-27, 28-30, 31-36, 37-47: pending, tracked in the program todo list.
- Repairs: none yet (Part 42 policy — bounded, reproduced defects only, before/after evidence).
