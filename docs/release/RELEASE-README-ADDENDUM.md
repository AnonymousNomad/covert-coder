# Release README addendum

Use this text alongside the certified candidate README when publishing the
source-release record. It is kept outside the immutable candidate so the
certification SHA remains unchanged.

> **Certified source core:** `dc0d30ee226e7ff822592e3a800f064b4441b7af`.
> The source core is independently certified. The packaged desktop installer
> is not certified, the permanent Resident is pending qualification, and the
> Capability Fabric and delegation runtimes remain post-candidate.

Start with [the source quickstart](SOURCE-QUICKSTART.md). Verify the downloaded
source archive and its companion SBOM with [the release verification guide](RELEASE-VERIFICATION.md).

The release claim matrix remains authoritative: a route or UI surface is not a
claim of readiness unless the matrix classifies it as proven.
