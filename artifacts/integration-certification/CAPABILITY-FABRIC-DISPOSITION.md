# CAPABILITY FABRIC v0.1 — DISPOSITION

Status: REGISTERED as the next bounded v1 release-extension task, per its own sequencing rule ('Do not derail the current candidate dependency chain').

Current candidate chain state (this lane): D2 positive+negatives, process ownership (Wave 10A), non-Git, workflow creation, stage->context, OpenCode Go through Covert (P0.3), and the real governed external mission (P0.4) are proven. Remaining before candidate freeze: P0.5 skills, P0.6 evidence transitions, P0.7 handoff+restart, P0.8 local<->external<->local, P0.9 offline, P0.10 delegation baseline, P1 items.

Fabric v0.1 scope (binding when started): one versioned capability manifest (fail-closed parsing), typed descriptors (TOOL/SKILL_PACK/WORKFLOW_PACK/VERIFIER/PROVIDER_ADAPTER/MODEL_ADAPTER/CONTEXT_PROVIDER/COMPOSITE_PACK), permission model separated from installation (AVAILABLE/INSTALLED/ENABLED/PERMISSION_GRANTED/HEALTHY/AUTHORIZED_FOR_OPERATION) integrated with canonical Authority, permission-diff on update, local capability registry with bounded semantic discovery (never dumped into prompts), read-only Trust Passport backend (UNKNOWN stays UNKNOWN), self-test/conformance states (Veritas remains authoritative), Helix provenance linkage, capability/interface dependencies (replaceable implementations), developer CLI (validate/verify, create if bounded), one or more reference fixtures/packs.

Reuse law: consume the existing plugin routes/trust semantics already in production (routes/plugins.ts + the plugins surface) as the substrate; no competing plugin semantics, no marketplace, no WASM sandbox, no remote registry in v0.1.
