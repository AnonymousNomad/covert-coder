# ARCHAEOLOGY EVIDENCE — wiring audit first pass

Target: `covert-production` @ `8ea6c8b`. Read-only traces; no servers or tests were run during archaeology.
Evidence for Luna: raw claims with file:line. Challenge everything.

## A. Micro-expert consumer chain (exact)

- Registry/loader: `harness/micro-experts.mjs:96` (`createExpertRegistry`), `:189` (`inferSync`), `:347` (`allocate`); featurizers `harness/expert-featurizers.mjs:1`.
- Routes: `node/src/routes/experts.ts:201`; wired `openapi.ts:68,514,733`; policy `operation-policy.mjs:118`.
- Manifests on disk: `.aide/experts/{task-router,diff-risk-gate,request-intent-classifier}.json`.
- In-loop consumer: `openapi.ts:749-772` (`consultExpert`) → `routes/agent.ts:118` → system-prompt block `agent.ts:124-139`.
- GATE: `agent.ts:107` requires `expertAdvisory && chatFnOverride`; `chatFnOverride` set only for `chat_source==='provider'` (`agent.ts:86-99`). Sovereign UI sends `chat_source:'local'` (`browser/src/cockpit/ResidentCore.ts:217`). → in-loop advisory bypassed on primary UI path. Known debt: `docs/phase0/RISK_REGISTER.md:17`.
- Timeout 200ms + silent catch (`agent.ts:113,122`); confidence ≤0.3 filtered (`openapi.ts:767`).
- diff-risk + classify-request: no production consumers (only `tests/arch/expert-serve-wirein.test.ts:94,101`); legacy shell calls intent only (`app.js:177-181`).
- Signals: `.aide/experts/signals.json` via `micro-experts.mjs:212,325-333`. No audit-bus event for advisory results.
- Freeze/thaw/prune: `experts.ts:282,296`, `micro-experts.mjs:386-403`; prune not routed (`:405`).

## B. Skills wiring (exact)

- Loader: `node/src/services/skills-loader.mjs:10-21` (MIN_SCORE 2.5, MAX_SKILLS 3, 1200-char excerpts), `:76-119`.
- Registry: `skills/registry.json` (294 skills) generated by `scripts/sync-skills.mjs:41-70` (manual, not in test chain).
- Injection: chat `chat-context.ts:170-174` → scaffold `:187`; agent `agent-loop.mjs:291-298` via `buildAdvisoryContext` `:156-174`.
- DEAD field: `skills-loader.mjs:100` reads `stage`; every caller passes one arg (`openapi.ts:478,561`, `agent-loop.mjs:293`); registry has 0 stage fields.
- Observability: agent emits context events (`agent-loop.mjs:300-307`); chat has no skills-specific token accounting (`chat-context.ts:198-216`).
- No outcome feedback: registry static; failure never re-scores.

## C. Workflow wiring (exact)

- Contract: `common/contracts/workflow.ts:28` (successor map), `:105`.
- Service: `workflow-service.ts:103-115` (per-stage outputs), `:247-286` (kernel gate), `:351` (create — zero production callers), `:377-423` (authority transition, write-ahead audit, rebuildFromAudit).
- Routes: `node/src/routes/workflow.ts:67-123`; policy `operation-policy.mjs:134-135`.
- No `.aide/workflow/state.json` on disk; `GET` returns null; `POST transition` fails NOT_FOUND without state.
- Stage branches exist only in the gate and Resident facts (`resident.ts:142-154,179-197`). No agent/chat/router code branches on stage.
- Legacy twin: `daemon/workflow.mjs:3` `WorkflowManager` (plan/patch/apply) reachable only via legacy port; facade maps `/api/workflow` to TS.

## D. Resident + context (exact)

- Resident routes/service: `resident.ts:245,640-666`; awareness provider `resident-awareness-provider.mjs:146`; envelope `resident-envelope.mjs:186-267`; SOPs `resident-sops.mjs:81-184`.
- Chat envelope compile: `chat-context.ts:106-219` (scaffold `:123`, learned `:126-129`, memory `:132-143`, RAG `:147-148`, recall `:154-161`, awareness `:163-185`, single system injection `:187`, drift reminder `:189-193`).
- Agent-loop seed: `agent-loop.mjs:106-154,286-298` — no awareness/memory/RAG.
- Resident worker bridge: `resident-worker-bridge.mjs:34-90`; sole importer experiment `experiments/resident-awareness/09-worker-loop.mjs:18,98-144`.
- Stale test: `tests/arch/resident-envelope.test.ts:250-267` asserts production files do not import the envelope; `openapi.ts:83` imports the provider which imports it (`resident-awareness-provider.mjs:24`) — the assertion checks a literal string and passes while the envelope is live.

## E. LEGACY CONTEXT BYPASS LIST (deliverable)

| # | Path | Classification |
|---|---|---|
| 1 | `daemon/server.mjs:479-508` legacy `/api/chat` (scaffold+learned+drift only) — shadowed via route map | legacy |
| 2 | `daemon/operator.mjs:6-24` `/api/operator` raw tree + 8 turns — absent from route map → served by legacy in production | legacy |
| 3 | `daemon/workflow.mjs:7-10` plan/apply prompts — shadowed by TS `/api/workflow` | legacy |
| 4 | `daemon/handoff.mjs:5-15` model-to-model raw handoff — capability absent from TS stack | legacy/shadowed |
| 5 | `daemon/arena-manager.mjs:33` bare `/chat/completions` | legacy internal |
| 6 | `agent-loop.mjs:286-298` second composer (no memory/RAG/awareness) | intentional, but integration gap |
| 7 | `agent.ts:107-143` expert advisory prepended directly | intentional |
| 8 | `telegram-brain.mjs:47-52` grants block + raw prompt | intentional isolation |
| 9 | `chat.ts:82` whole conversation sent each request (trimmed `model-router.ts:240`) | intentional by contract |
| 10 | `handoff-service.mjs:177-229` raw transcript stored for export tiers | intentional (never model input) |
| 11 | Overlapping injections into one system slot (learned + memory + recency + recall + awareness); dedupe only inside envelope (`resident-envelope.mjs:269-295`) | intentional but unguarded (defect risk) |
| 12 | `harness/memory-blocks.mjs:36-50` writer with zero production callers | ORPHANED |

## F. Memory store map + restart survival

| Store | Written | Read | Consumer | Restart |
|---|---|---|---|---|
| `.aide/workflow/state.json` | `workflow-service.ts:191,217-234` | `:193-215` | route/resident/gates | survives |
| `.aide/chat-history.json` | `chat-store.ts:43-70` | `:21-33` | chat history route/UI | survives |
| `.aide/memory/sessions.jsonl` | `memory-recall.mjs:210-220` | `:115-133` | chat recall | survives |
| `.aide/memory/blocks/*.md` | no production writer | `memory-blocks.mjs:25-33` | chat | survives |
| `.aide/memory/days/*.json` | `memory-spine.mjs:103-104` | `routes/memory.ts:65-70` | chat recency | survives |
| `.aide/memory/patterns.jsonl` | `helix-join.mjs:21,44` | `system-map.mjs:110` | status card only | survives |
| `.aide/cipher-state.jsonl` | `audit-trail.mjs:38,92` | `memory-spine.mjs:8,34` | digests + `[learned]` | survives |
| `.aide/trajectories` | `agent-loop.mjs:582-590` | harvest only | training | survives (absent now) |
| `.aide/verifications` | `agent-loop.mjs:628-638` | workflow gate | release gate | survives |
| `.aide/handoff`/`imported` | `handoff-service.mjs:231-239,287-298` | API read only | none | storage only |
| `.aide/agent-loop-sessions` | NO WRITER | `system-map.mjs:110-118` | status card | orphaned |
| `.aide/checkpoints/repo` | `agent-checkpoints.mjs:113-135` | `:180-187` | snapshot only | survives |
| in-memory agent sessions | `agent-loop.mjs:234` | Map | routes | LOST |
| resident decisions buffer | `resident.ts:247` | route | UI | LOST |
| awareness journal | `resident-awareness-provider.mjs:161` | in-memory | envelope | LOST |

## G. Authority gaps (P0 evidence)

1. `server.ts:382-399` — self-improve runner: detached, `stdio:'ignore'`, unref'd; no authority prepare/decide; writes `scripts/selfimprove.mjs:121-156,184-196`.
2. `workflow-service.ts:275-279` + `workflow-validators.ts:217-236` — abstain passes; records unauthenticated; `.aide/` risk-flag only (`agent-tools.mjs:9,83-88`).
3. `operation-policy.mjs:86-92` — chat external egress auto-approved as read; `routes/modelhub.ts:57-68` HF search with token.
4. `openapi.ts:788-808` — direct tool dispatch without authority/evidence.

## H. Routing inputs — real vs absent

REAL: role; local manifest roles (`model-router.ts:172-186`); engine liveness probe (`:145-164`); caller `modelId` (`routes/chat.ts:85,103`); cloud catalog + stored-key status (`providers.ts:17-72`); BYOK plan/act/utility + local-only (`openapi.ts:750-757`, `provider-connections.mjs:219-224`); hardware probe (`hardware.ts:32-58`); RAM/VRAM fit at ingest/start (`model-fit.ts:90-125`, `model-runtime.ts:401-446,816-817`); HF token for catalog (`modelhub.mjs:76-87`).
ABSENT: task/content awareness; capability metadata (`capabilities: []` `model-router.ts:88,108`); policy input; hardware profile inside router; resource fit in selection; subscription transport routing (presence-only, `routing_available:false`); task-aware HF discovery; connectivity probe.
